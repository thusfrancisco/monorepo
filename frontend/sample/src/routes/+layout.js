// For SSG. This can be false if you're using a fallback (i.e. SPA mode)
export const prerender = true;
// Redirect "routes/about" to an index.html file in an "/about" directory
export const trailingSlash = 'always';
